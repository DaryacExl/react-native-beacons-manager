{
  "include": [
    "lib/**/*.js" // Include all JavaScript files in the lib directory, recursively
  ],    
  "exclude": [
    "node_modules",
    "android",
    "ios",
    "images",
    "example"
  ]
}
